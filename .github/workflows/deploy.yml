name: Deploy
on: [push]
jobs:
  terraform:
    runs-on: ubuntu-latest
    defaults:
      run:
        shell: bash
        working-directory: ./terraform
    steps:
      - name: checkout
        uses: actions/checkout@v2

      - name: setup teraform
        uses: hashicorp/setup-terraform@v1
        with:
            terraform_version: 1.1.6

      - name: run tf-init
        id: init
        working-directory: ./terraform
        run: echo "something is still there: ${{ secrets.SOMETHING }}"


